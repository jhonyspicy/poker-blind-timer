# Tasks: Blind Timer MVP

> 2026-07-29: トップページ起点の段階的リビルド(design.md D10)に合わせて再構成。
> エディタ / サイネージの旧実装は雛形化したため、該当タスクは未完了へ戻した。

## 1. プロジェクト基盤

- [x] 1.1 Vite + React + TypeScript プロジェクトを作成(strict、`base: '/poker-blind-timer/'`、ESLint + Prettier)
- [x] 1.2 React Router を導入し `/editor` `/signage` `/remote` の 3 ルートとプレースホルダーページを作成(404.html フォールバック含む)
- [x] 1.3 Vitest をセットアップし、サンプルテストが通ることを確認
- [x] 1.4 GitHub Actions で GitHub Pages への自動デプロイを構築し、トップページを含む 4 つの URL が実際に開けることを確認
- [x] 1.5 README.md の技術スタック・セットアップ・開発用コマンドを更新

## 2. ドメインモデルとタイマーエンジン

- [x] 2.1 型定義を作成(TournamentConfig、Level/Break、Prize、History(entry/addon/bust + chip)、TimerState の判別可能ユニオン)
- [x] 2.2 タイマーエンジンを実装(基準時刻差分方式、一時停止/再開、レベル自動遷移の冪等性、次ブレイク・レイトレジ締切までの残り時間算出)
- [x] 2.3 タイマーエンジンの単体テストを作成(自動遷移、一時停止中の時間停止、非アクティブ後の追いつき、最終レベル終了)
- [x] 2.4 統計導出ロジックを実装(履歴 → 総エントリー数/現在プレイヤー数/アドオン数/平均チップ、履歴の追加・修正・削除)し、単体テストを作成
- [ ] 2.5 新データモデルの未決項目(created_at、プライズ金額型、秒単位化)を確定し、必要ならドメイン型を改訂する(design.md Open Questions / D11・D12 参照)
- [x] 2.6 レイトレジ締め切りをストラクチャー項目(lateRegClose マーカー、1 つまで)へ変更し、タイマーエンジンを対応させる(単体テスト含む)
- [x] 2.7 設定からスターティングスタック / アドオン設定を廃止する(チップ量は記録時にリモコンから入力する方針に統一)

## 3. 永続化(IndexedDB)

- [x] 3.1 `idb` を導入し、`configs` / `session` ストアのアクセス層を実装
- [x] 3.2 設定の保存・一覧・読み込み・削除とタイマー状態スナップショットの保存・復元を実装(単体テスト含む)
- [x] 3.3 `room` ストア(店舗情報 1 件)を追加し、DB v2 マイグレーションで v1 の設定ごとの店名(shopName)を引き継ぐ(単体テスト含む)
- [x] 3.4 DB v3 マイグレーション: lateRegEndIndex をマーカー項目へ変換し、廃止フィールドを除去、進行中セッションの levelIndex を追従(単体テスト含む)

## 4. トップページ

- [x] 4.1 設定一覧 UI を実装(更新日時順、開始/編集/複製/削除、削除確認ダイアログ、空状態、トースト)し、IndexedDB と接続
- [x] 4.2 店名の表示とインライン編集(テキストボックス+キャンセル/保存)を実装し、`room` ストアへ保存
- [x] 4.3 デザインモック(blind-timer-admin-v2、Nocturne DS + ゴールドアクセント)に基づくスタイルを CSS Modules で実装
- [x] 4.4 開始/再開のペアリングモーダルを実装(少々お待ちください → チャンネル発行+Ably 接続 → QR+URL 表示 → presence でリモコン接続検知 → 「ブラインドタイマーを開く」→ サイネージ遷移+フルスクリーン)
- [x] 4.5 「再開」の表示条件を実装(直近セッションが該当設定のもので、優勝確定・終了していない場合)

## 5. エディタ画面(作り直し)

- [x] 5.1 ストラクチャー編集 UI を実装(レベル追加/編集/削除/並び、ブレイク挿入、レイトレジ締め切りマーカーの挿入(1 つまで))
- [x] 5.2 トーナメント情報の入力 UI を実装(タイトル、プライズ一覧、エントリー案内。店名はトップページの管理に移動済み)
- [x] 5.3 入力検証を実装(レベル 1 つ以上、SB/BB/継続時間は正の数、レイトレジマーカーは 1 つまで)
- [x] 5.4 設定の保存と、トップページからの編集遷移(対象設定を読み込んだ状態で開く)を実装し、IndexedDB と接続

## 6. サイネージ画面(作り直し)

- [x] 6.1 ドメイン拡張: タイマー状態に「開始前(waiting)」を追加し、セッションにチャンネル ID を保持。優勝確定(現在人数 1)の導出を実装(単体テスト・DB v4 マイグレーション含む)
- [ ] 6.2 待機画面を実装(`three` 導入、チップ浮遊背景モックの移植、店名・トーナメント名・最初のブラインド)
- [ ] 6.3 タイマー画面の表示を実装(1920×1080 スケーリング、Canvas 円形ゲージ、LEVEL バッジ、BLINDS/ANTE/NEXT、PRIZE LIST、PLAYERS/ADD-ON/AVERAGE STACK/NEXT BREAK/LATE REGISTRATION、WebGL 背景の移植)
- [ ] 6.4 レベルアップ演出・一時停止テープ演出を移植し、タイマー進行と接続(prefers-reduced-motion 対応)
- [ ] 6.5 ブレイク画面・優勝画面を実装(デザイン未作成のためプレースホルダー。エントリー案内の表示を含む)
- [ ] 6.6 演出動画オーバーレイ基盤を実装(イベント→動画マッピング、mp4 / webm+ogg の 2 形式再生、素材未配置時はスキップ、各イベント 1 回だけ再生)
- [ ] 6.7 状態の IndexedDB 保存とリロード復元を実装(4 画面の復元、保存済みチャンネル ID での再接続、進行中・一時停止中の両方)
- [ ] 6.8 フルスクリーン切り替えと QR 再表示を実装

## 7. Cloudflare Worker(接続 URL 発行・トークン認証)

- [x] 7.1 `worker/` に Worker プロジェクトを作成(wrangler、TypeScript)
- [x] 7.2 `POST /session`(128bit ランダムなチャンネル ID 発行)と `GET /token?ch=<id>`(チャンネル限定の Ably TokenRequest 発行)を実装(CORS 設定含む)
- [ ] 7.3 Worker をデプロイし、Ably API キーを secret に設定。フロントから `VITE_PAIRING_API_URL` で参照できるようにする

## 8. リアルタイム同期とペアリング

- [x] 8.1 メッセージ型定義を更新(command: START / HISTORY_ADD / HISTORY_UPDATE / HISTORY_DELETE / PAUSE / RESUME / NEXT_LEVEL / PREV_LEVEL / REQUEST_STATE、TITLE_UPDATE の削除、requestId による重複排除、state: histories を含むスナップショット)
- [ ] 8.2 Ably 接続層を更新(トークン認証 authUrl、チャンネル購読/発行、presence(リモコン入室の宣言と検知)、再接続処理、購読解除によるリーク防止)
- [ ] 8.3 サイネージ側を実装: 保存済みチャンネルでの接続、コマンド受信と状態への適用、状態変化時の state publish
- [ ] 8.4 リモコン画面を実装: `?ch=` からの接続(なし/失敗時の案内)、presence 入室、操作ボタン(トーナメント開始、エントリー/バスト/アドオンの記録、チップ量の入力、一時停止/再開、レベル移動)、履歴一覧の表示と id 指定の修正・削除、受信 state による現在状態表示
- [ ] 8.5 スマホ向け UI 調整(片手操作、誤タップ防止、影響の大きい操作の分離配置)

## 9. 統合確認とリリース

- [ ] 9.1 エンドツーエンドの手動確認: トップページで設定作成 → エディタで編集 → 開始モーダル → QR でスマホ接続 → 「ブラインドタイマーを開く」→ リモコンから開始 → 全操作 → 優勝まで → リロード復元(再開含む)→ 切断/再接続
- [ ] 9.2 UI 確認(16:9 フルスクリーン、1920×1080 相当、長いトーナメント名、大きなブラインド数値、1 時間超のタイマー)
- [ ] 9.3 Lint・型チェック・テスト・ビルドがすべて通ることを確認し、GitHub Pages 本番 URL で動作確認
- [ ] 9.4 README.md を最終更新(使い方、デプロイ手順、Worker のセットアップ手順、スリープ無効の推奨)
