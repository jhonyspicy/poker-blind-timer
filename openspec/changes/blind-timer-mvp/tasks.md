# Tasks: Blind Timer MVP

## 1. プロジェクト基盤

- [x] 1.1 Vite + React + TypeScript プロジェクトを作成(strict、`base: '/poker-blind-timer/'`、ESLint + Prettier)
- [x] 1.2 React Router を導入し `/editor` `/signage` `/remote` の 3 ルートとプレースホルダーページを作成(404.html フォールバック含む)
- [x] 1.3 Vitest をセットアップし、サンプルテストが通ることを確認
- [ ] 1.4 GitHub Actions で GitHub Pages への自動デプロイを構築し、3 つの URL が実際に開けることを確認
- [x] 1.5 README.md の技術スタック・セットアップ・開発用コマンドを更新

## 2. ドメインモデルとタイマーエンジン

- [x] 2.1 型定義を作成(TournamentConfig、Level/Break、Prize、History(entry/addon/bust + chip)、TimerState の判別可能ユニオン)
- [x] 2.2 タイマーエンジンを実装(基準時刻差分方式、一時停止/再開、レベル自動遷移の冪等性、次ブレイク・レイトレジ締切までの残り時間算出)
- [x] 2.3 タイマーエンジンの単体テストを作成(自動遷移、一時停止中の時間停止、非アクティブ後の追いつき、最終レベル終了)
- [x] 2.4 統計導出ロジックを実装(履歴 → 総エントリー数/現在プレイヤー数/アドオン数/平均チップ、履歴の追加・修正・削除)し、単体テストを作成

## 3. 永続化(IndexedDB)

- [x] 3.1 `idb` を導入し、`configs` / `session` ストアのアクセス層を実装
- [x] 3.2 設定の保存・一覧・読み込み・削除とタイマー状態スナップショットの保存・復元を実装(単体テスト含む)

## 4. エディタ画面

- [x] 4.1 ストラクチャー編集 UI を実装(レベル追加/編集/削除/並び、ブレイク挿入、レイトレジ締切位置)
- [x] 4.2 トーナメント情報の入力 UI を実装(店名、タイトル、プライズ一覧、スターティングスタック、アドオン/リバイ設定とアドオンのチップ量)
- [x] 4.3 入力検証を実装(レベル 1 つ以上、SB/BB/継続時間/スターティングスタックは正の数、アドオン可ならチップ量も正の数)
- [x] 4.4 設定の保存・一覧・読み込み・削除 UI を実装し、IndexedDB と接続

## 5. サイネージ画面

- [x] 5.1 保存済み設定の読み込みとタイマー起動を実装(設定がない場合はエディタへの案内)
- [x] 5.2 16:9 フルスクリーンレイアウトを実装(中央に残り時間、ブラインド、次ブラインド、店名、タイトル、プライズ、履歴から導出した統計(人数/エントリー/アドオン/平均チップ)、次ブレイク・レイトレジ残り時間)
- [x] 5.3 ブレイク中・レイトレジ終了・トーナメント終了の表示を実装
- [x] 5.4 タイマー状態の IndexedDB 保存とリロード復元を実装(進行中・一時停止中の両方)
- [x] 5.5 フルスクリーン切り替えボタンを実装

## 6. Cloudflare Worker(接続 URL 発行・トークン認証)

- [x] 6.1 `worker/` に Worker プロジェクトを作成(wrangler、TypeScript)
- [x] 6.2 `POST /session`(128bit ランダムなチャンネル ID 発行)と `GET /token?ch=<id>`(チャンネル限定の Ably TokenRequest 発行)を実装(CORS 設定含む)
- [ ] 6.3 Worker をデプロイし、Ably API キーを secret に設定。フロントから `VITE_PAIRING_API_URL` で参照できるようにする

## 7. リアルタイム同期とペアリング

- [ ] 7.1 メッセージ型定義を作成(command: HISTORY_ADD / HISTORY_UPDATE / HISTORY_DELETE / PAUSE / RESUME / NEXT_LEVEL / PREV_LEVEL / TITLE_UPDATE / REQUEST_STATE、requestId による重複排除、state: histories を含むスナップショット)
- [ ] 7.2 Ably 接続層を実装(トークン認証 authUrl、チャンネル購読/発行、再接続処理、購読解除によるリーク防止)
- [ ] 7.3 サイネージ側を実装: セッション発行 → QR コード表示(タイマー表示中の再表示含む)、コマンド受信と状態への適用、状態変化時の state publish
- [ ] 7.4 リモコン画面を実装: `?ch=` からの接続(なし/失敗時の案内)、操作ボタン(エントリー/バスト/アドオンの記録、チップ量の上書き入力、一時停止/再開、レベル移動、タイトル変更)、履歴一覧の表示と id 指定の修正・削除、受信 state による現在状態表示
- [ ] 7.5 スマホ向け UI 調整(片手操作、誤タップ防止、影響の大きい操作の分離配置)

## 8. 統合確認とリリース

- [ ] 8.1 エンドツーエンドの手動確認: エディタで設定 → サイネージ起動 → QR でスマホ接続 → 全操作 → リロード復元 → 切断/再接続
- [ ] 8.2 UI 確認(16:9 フルスクリーン、1920×1080 相当、長いトーナメント名、大きなブラインド数値、1 時間超のタイマー)
- [ ] 8.3 Lint・型チェック・テスト・ビルドがすべて通ることを確認し、GitHub Pages 本番 URL で動作確認
- [ ] 8.4 README.md を最終更新(使い方、デプロイ手順、Worker のセットアップ手順、スリープ無効の推奨)
