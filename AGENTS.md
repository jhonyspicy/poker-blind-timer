# AGENTS.md

このリポジトリを編集する AI コーディングエージェント向けの開発指示書です。

## 1. プロジェクト概要

Blind Timer は、ポーカートーナメント運営で繰り返し使用するブラウザベースのブラインドタイマーです。TypeScript で実装し、GitHub Pages 上の静的サイトとして提供します。

- **エディタ**（`/editor`）: PC のブラウザ。ストラクチャー等を設定しローカルに保存。
- **サイネージ**（`/signage`）: PC のブラウザ。プレイヤー向けの大画面フルスクリーン表示。
- **リモコン**（`/remote`）: スマホのブラウザ。運営者が操作。
- **リアルタイム同期**: Ably（pub/sub）。リモコンが publish、サイネージが subscribe。
- **接続用 URL の発行**: Cloudflare Workers（Ably のトークン認証もここで担う想定）。
- **ローカル保存**: IndexedDB（設定・ストラクチャーの保存、リロード耐性、再利用）。
- **ペアリング**: サイネージが QR コードでリモコン用 URL を表示し、スマホで読み取って接続。

自前サーバーは立てず（Cloudflare Workers を除く）、できるだけミニマムな構成を目指します。

**最優先ドキュメント:** 機能範囲・システム構成・技術スタックの正は [docs/spec.md](docs/spec.md) です。この文書や README.md と矛盾する場合は docs/spec.md に従ってください。

**現在の状態:** 設計段階です。実装コードはまだ存在しません。仕様の作成・変更には OpenSpec（`openspec/`）のワークフロー（`/opsx:propose` など）を使用してください。

## 2. 開発の優先順位

以下の順番を基本とします。

1. タイマーとして正確に動作すること
2. プレイヤーが情報を瞬時に理解できること
3. 運営者がスマホから簡単・確実に操作できること
4. リロードや通信切断からの復元が安全であること
5. 保守しやすく拡張しやすいこと

見た目の凝った作り込みよりも、タイマーの正確性・同期の確実性・情報の視認性を優先してください。

## 3. UI・デザイン方針

### サイネージ画面（PC・大画面）

- 16:9 のフルスクリーン表示を前提とする
- テレビや店舗モニターから離れて見ても読める UI にする
- 中央の残り時間を最も目立たせる
- 文字や数値を小さくしすぎない
- 情報量を詰め込みすぎない
- デザイン / レイアウトは既存のものを使用する（docs/spec.md 参照）
- `vw`、`vh`、`vmin`、`clamp()` などを適切に使用する
- ブラウザのフルスクリーン API を利用する場合は、ユーザー操作が必要な制約を考慮する

### コントローラー画面（スマホ）

- スマホでの片手操作を前提とする
- 誤タップしにくいボタンサイズ・配置にする
- 一時停止やレベル移動など、影響の大きい操作は誤操作しにくくする
- 現在の状態（残り時間、レベル、人数など）が手元でも確認できるようにする

## 4. タイマー実装の方針

タイマーは画面描画回数を単純にカウントして実装しないでください。以下を基準にしてください。

- 経過時間は基準時刻との差分から算出する
- `setInterval` の実行回数に依存しない
- タブの非アクティブ化や処理落ちによるズレを防ぐ
- 一時停止時には残り時間を保持する
- 再開時には新しい基準時刻を設定する
- レベル切り替えを重複して実行しない
- タイマーの進行は**サイネージ側を正（source of truth）**とし、状態を保持する
- リロード時は IndexedDB から状態を復元する
- 表示側と操作側で状態の矛盾が起きない設計にする

## 5. リアルタイム同期の方針

- サイネージ = 表示側（subscribe）、コントローラー = 操作側（publish）とする
- 1 つの Ably チャンネルを使用し、URL に含めたチャンネル識別子で両者を接続する
- 操作メッセージは種類を明確に定義する（例: `PLAYER_COUNT_UPDATE`、`ADDON_UPDATE`、`PAUSE` / `RESUME`、`NEXT_LEVEL` / `PREV_LEVEL`、`TITLE_UPDATE`）
- メッセージ形式は拡張しやすい構造にする
- 通信切断・再接続時の挙動を考慮する（サイネージ側の状態が正）
- 重複配信や順序入れ替わりが起きても矛盾しない設計にする

## 6. 状態管理

タイマーの状態は、可能な限り明確な状態として管理してください。想定される状態例:

```text
setup        # 設定モード
pairing      # QR コード表示・コントローラー接続待ち
running      # タイマー進行中
paused       # 一時停止中
break        # ブレイク中
finished     # トーナメント終了
```

実際の実装に合わない場合は、既存設計を尊重して調整してください。

複数の Boolean だけで状態を表現し、矛盾した状態が同時に成立する設計は避けてください。

## 7. コーディング方針

リポジトリの既存コード、Lint、Formatter、命名規則を最優先してください。加えて以下を守ってください。

- 既存の設計を確認してから変更する
- 必要以上のリファクタリングを行わない
- 1 つの変更で無関係なコードを大量に変更しない
- UI コンポーネントとタイマーのロジックを分離する
- 同期メッセージの処理とトーナメント状態を密結合させない
- マジックナンバーを避ける
- 型が利用できる環境では型を明確にする
- 状態名、イベント名、コンポーネント名は意味が分かる名前にする
- コメントは「何をしているか」ではなく「なぜ必要か」を中心に書く
- 外部ライブラリを追加する前に、標準機能や既存依存で実現できないか確認する
- 大きな依存パッケージを安易に追加しない
- ブラウザ互換性を考慮する
- 長時間稼働してもメモリリークしないようにする（イベントリスナー、タイマー、Ably の購読を適切に解除する）

## 8. セキュリティ

- API キーや秘密情報を Git へコミットしない
- `.env` をコミットしない
- 公開用の環境変数と秘密情報を区別する
- Ably は秘密の API キーをブラウザに配布せず、トークン認証を使用する
- チャンネル識別子は推測されにくい値にする
- ユーザー入力をそのまま HTML として描画しない
- `innerHTML` の使用は必要最小限にする

## 9. MVP の考え方

現段階では docs/spec.md に定義されたミニマム構成を優先してください。

- 将来機能（演出強化、フォトモード、SaaS 化など）のために複雑すぎる仕組みを先に作らない
- ただし、将来の拡張を完全に妨げる密結合な実装は避ける
- まずは実際のトーナメント運営で使える品質を目指す
- 動作するシンプルな実装を、過剰に抽象化された実装より優先する
- ユーザー登録、課金、店舗管理などは現在のスコープに含めない

## 10. 変更時の確認事項

コードを変更した後は、リポジトリに存在する範囲で以下を実行してください。

- Formatter
- Lint
- 型チェック
- テスト
- ビルド

現時点ではこれらのコマンドはまだ存在しません。存在しないコマンドを作ったふりをせず、実行できなかったことを報告に明記してください。コマンドが整備されたら、このセクションと README.md を更新してください。

UI を変更した場合は、最低限以下を確認してください。

- サイネージ: 16:9、1920×1080 相当、フルスクリーン
- コントローラー: スマホ相当の画面幅
- 長いトーナメント名・店舗名
- 大きなブラインド数値
- タイマーが 1 時間を超える場合
- 一時停止 / 再開
- レベル切り替え（進む / 戻る）
- ブレイクへの切り替え
- リロード後の状態復元

## 11. アップデート情報の更新

利用者に見える変更(画面・操作方法・挙動の変化)を実装したら、トップページの「アップデート情報」に必ず追記してください。データは `src/pages/home/updates.ts` にあります。

- `ALL_UPDATES` 配列の**先頭**にエントリを追加する
- `date` は作業日(YYYY-MM-DD)、`text` は利用者向けの「〜しました」調の説明文にする(開発者向けの用語やファイル名は書かない)
- 掲載は直近 10 件まで。表示側は `MAX_UPDATES` で自動的に 10 件に制限されるが、10 件を超えた古い行はファイルからも削除する
- リファクタリング・内部実装のみの変更・ドキュメント修正など、利用者に見えない変更では追記しない

## 12. AI エージェントへの指示

作業する際は、以下の手順を基本としてください。

1. docs/spec.md と関連ファイルを確認する
2. 現在の設計と命名規則を把握する
3. 変更範囲を最小限に決める
4. 実装する
5. テスト、Lint、型チェック、ビルドを実行する
6. 実行できなかった確認項目を明示する
7. 変更内容と確認結果を簡潔に報告する

要件が曖昧でも、docs/spec.md とプロジェクト方針から合理的に判断できる場合は、作業を止めずに実装してください。

ただし、以下に該当する場合は勝手に決定しないでください。

- データ削除を伴う変更
- 認証や権限に関わる変更
- API の破壊的変更
- 大規模な依存パッケージ追加
- 全体設計を変更する大規模リファクタリング
- docs/spec.md のスコープを超える機能追加
