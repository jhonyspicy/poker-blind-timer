# CLAUDE.md

開発指示は @AGENTS.md を参照してください。

特に忘れやすい点:

- **利用者に見える変更をしたら、`src/pages/home/updates.ts` の「アップデート情報」に必ず追記する**(AGENTS.md「アップデート情報の更新」参照。直近 10 件まで、古い行は削除)
- 仕様の正は `openspec/` 配下です(docs/spec.md は廃止済み)
- **仕様に関わる変更(画面・挙動・同期プロトコル・データモデル)を実装したら、同じ作業の中で openspec/ も更新する**:
  - 小さな修正: `openspec/specs/` の該当 capability の仕様を直接更新する(アクティブなチェンジがあればそちらのデルタ仕様を更新する)
  - まとまった機能追加: `/opsx:propose` でチェンジを起こしてから実装し、完了後に sync / archive する
  - 内部リファクタリング・ドキュメントのみ・依存更新などの変更では不要
